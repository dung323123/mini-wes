services:
  postgres:
    image: postgres:16-alpine
    container_name: mini-wes-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: miniwes
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - mini_wes_pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d miniwes"]
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  mini_wes_pg_data:
